* Have commands specify required arguments, and parse automatically?
* Move away from discord-hs, it's really bad
* * Can't handle large output
* * Crashes randomly (partiality in MY haskell??)
* Implement persistent/etc
